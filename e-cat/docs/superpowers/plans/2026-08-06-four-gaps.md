# 四项缺口补全 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补全四项能力缺口：metrics 自动端点、openapi 全 HTTP 方法、config-remote Consul watch、ClickHouse TsdbClient。

**Architecture:** 四个功能分布在四个独立 crate，互不依赖。metrics 端点在 `ecat-metrics` 提供 axum Router，由 `ecat-transport-http` 的 `HttpServer::start()` 自动 merge；openapi 扩展 `PathItem` 字段与 `add_route` 匹配；config-remote 用 Consul blocking query（index + wait）实现 watch，mpsc channel 输出；clickhouse 在 `ClickhouseClient` 上新增 `TsdbClient` impl（自动建表 + JSONEachRow 写入）。

**Tech Stack:** Rust 2024 / axum 0.8 / tower 0.5 / prometheus 0.13 / reqwest 0.12 / tokio / async-trait / serde_json。工作区根 `/home/wwwroot/e-cat`。

**Spec:** `docs/superpowers/specs/2026-08-06-four-gaps-design.md`（已批准）

---

## 文件结构

| 文件 | 职责 | 操作 |
|---|---|---|
| `ecat-openapi/src/lib.rs` | PathItem 字段扩展 + add_route 匹配 | 修改 |
| `ecat-metrics/Cargo.toml` | + axum、tower(dev) | 修改 |
| `ecat-metrics/src/lib.rs` | `metrics_router()` | 修改 |
| `ecat-transport-http/Cargo.toml` | + ecat-metrics | 修改 |
| `ecat-transport-http/src/lib.rs` | `merged_router()` + start() 自动挂载 | 修改 |
| `ecat-config-remote/src/lib.rs` | fetch 重构 + watch | 修改 |
| `ecat-config-remote/Cargo.toml` | + axum(dev) | 修改 |
| `ecat-data-clickhouse/src/lib.rs` | `impl TsdbClient` + 纯函数 | 修改 |

全部为修改现有文件，无新建文件。

---

### Task 1: OpenAPI 全方法 — ecat-openapi

**Files:**
- Modify: `ecat-openapi/src/lib.rs`（PathItem 结构、add_route match、tests）

- [ ] **Step 1: 写失败测试**（追加到 `ecat-openapi/src/lib.rs` 的 `mod tests` 末尾）

```rust
    #[test]
    fn all_seven_methods_serialize() {
        let spec = OpenApiBuilder::new("M", "1")
            .add_route("/r", "GET", "g", vec![])
            .add_route("/r", "POST", "p", vec![])
            .add_route("/r", "PUT", "u", vec![])
            .add_route("/r", "DELETE", "d", vec![])
            .add_route("/r", "PATCH", "a", vec![])
            .add_route("/r", "HEAD", "h", vec![])
            .add_route("/r", "OPTIONS", "o", vec![])
            .build();
        let json = serde_json::to_value(&spec).unwrap();
        let item = json.pointer("/paths//r").unwrap().as_object().unwrap();
        for method in ["get", "post", "put", "delete", "patch", "head", "options"] {
            assert!(item.contains_key(method), "missing {method}");
        }
    }

    #[test]
    fn unknown_method_ignored() {
        let spec = OpenApiBuilder::new("M", "1")
            .add_route("/x", "TRACE", "t", vec![])
            .build();
        let json = serde_json::to_value(&spec).unwrap();
        let item = json.pointer("/paths//x").unwrap().as_object().unwrap();
        assert!(item.is_empty(), "unknown method must be ignored");
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test -p ecat-openapi --lib`
Expected: `all_seven_methods_serialize` FAIL（编译错误：`PathItem` 无 `put` 字段）

- [ ] **Step 3: 实现**（`ecat-openapi/src/lib.rs`）

`PathItem` 增加字段（在 `post` 字段后）：

```rust
    #[serde(skip_serializing_if = "Option::is_none")]
    pub post: Option<Operation>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub put: Option<Operation>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub delete: Option<Operation>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub patch: Option<Operation>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub head: Option<Operation>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub options: Option<Operation>,
```

`add_route` 内 `or_insert` 初始化补全字段：

```rust
        let entry = self.paths.entry(path.into()).or_insert(PathItem {
            get: None,
            post: None,
            put: None,
            delete: None,
            patch: None,
            head: None,
            options: None,
        });
```

`match method` 扩展（各分支 move `op`，一次调用仅一个分支命中）：

```rust
        match method {
            "GET" | "get" => entry.get = Some(op),
            "POST" | "post" => entry.post = Some(op),
            "PUT" | "put" => entry.put = Some(op),
            "DELETE" | "delete" => entry.delete = Some(op),
            "PATCH" | "patch" => entry.patch = Some(op),
            "HEAD" | "head" => entry.head = Some(op),
            "OPTIONS" | "options" => entry.options = Some(op),
            _ => {}
        }
```

- [ ] **Step 4: 运行确认通过**

Run: `cargo test -p ecat-openapi --lib`
Expected: 4 个测试全 PASS（含原有 2 个）

- [ ] **Step 5: 工作区编译检查**

Run: `cargo build`
Expected: 成功，无错误

- [ ] **Step 6: 提交**

```bash
git add ecat-openapi/src/lib.rs
git commit -m "ecat-openapi: 支持 PUT/DELETE/PATCH/HEAD/OPTIONS 方法"
```

---

### Task 2: Metrics 自动端点 — ecat-metrics + ecat-transport-http

**Files:**
- Modify: `ecat-metrics/Cargo.toml`、`ecat-metrics/src/lib.rs`
- Modify: `ecat-transport-http/Cargo.toml`、`ecat-transport-http/src/lib.rs`

- [ ] **Step 1: 改 ecat-metrics/Cargo.toml**

```toml
[dependencies]
prometheus = "0.13"
axum.workspace = true

[dev-dependencies]
tower.workspace = true
```

- [ ] **Step 2: 写失败测试**（追加到 `ecat-metrics/src/lib.rs` 的 `mod tests` 末尾）

```rust
    #[tokio::test]
    async fn metrics_router_serves_prometheus_text() {
        use tower::ServiceExt;
        let router = metrics_router();
        let resp = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/metrics")
                    .body(axum::body::Body::empty())
                    .unwrap(),
            )
            .await
            .unwrap();
        assert_eq!(resp.status(), axum::http::StatusCode::OK);
        let ct = resp
            .headers()
            .get(axum::http::header::CONTENT_TYPE)
            .unwrap()
            .to_str()
            .unwrap();
        assert!(ct.starts_with("text/plain"), "got content-type: {ct}");
    }
```

- [ ] **Step 3: 运行确认失败**

Run: `cargo test -p ecat-metrics --lib`
Expected: FAIL（编译错误：`metrics_router` 未定义）

- [ ] **Step 4: 实现**（`ecat-metrics/src/lib.rs`，`use` 区加 axum 导入，文件末尾 `mod tests` 前加）

```rust
use axum::Router;
use axum::response::IntoResponse;
use axum::routing::get;

pub fn metrics_router() -> Router {
    async fn handler() -> impl IntoResponse {
        (
            [(
                axum::http::header::CONTENT_TYPE,
                "text/plain; version=0.0.4; charset=utf-8",
            )],
            metrics_text(),
        )
    }
    Router::new().route("/metrics", get(handler))
}
```

- [ ] **Step 5: 运行确认通过**

Run: `cargo test -p ecat-metrics --lib`
Expected: 3 个测试全 PASS

- [ ] **Step 6: 改 ecat-transport-http/Cargo.toml**

`[dependencies]` 加一行：

```toml
ecat-metrics = { path = "../ecat-metrics" }
```

- [ ] **Step 7: 写失败测试**（追加到 `ecat-transport-http/src/lib.rs` 的 `mod tests` 末尾）

```rust
    #[tokio::test]
    async fn auto_mounts_metrics_endpoint() {
        use tower::ServiceExt;
        let srv = HttpServer::new("127.0.0.1:0");
        let resp = srv
            .merged_router()
            .oneshot(
                axum::http::Request::builder()
                    .uri("/metrics")
                    .body(axum::body::Body::empty())
                    .unwrap(),
            )
            .await
            .unwrap();
        assert_eq!(resp.status(), axum::http::StatusCode::OK);
    }
```

- [ ] **Step 8: 运行确认失败**

Run: `cargo test -p ecat-transport-http --lib`
Expected: FAIL（编译错误：`merged_router` 未定义）

- [ ] **Step 9: 实现**（`ecat-transport-http/src/lib.rs`）

`impl HttpServer` 内新增方法（`start` 之前）：

```rust
    /// 用户 router 与内置 /metrics 端点合并。
    /// /metrics 为框架保留路径：用户 router 若也定义该路径，merge 会 panic。
    fn merged_router(&self) -> Router {
        self.router
            .clone()
            .unwrap_or_default()
            .merge(ecat_metrics::metrics_router())
    }
```

`start()` 内第一行改为：

```rust
        let router = self.merged_router();
```

- [ ] **Step 10: 运行确认通过**

Run: `cargo test -p ecat-transport-http --lib`
Expected: 5 个测试全 PASS（含原 4 个）

- [ ] **Step 11: 工作区编译检查**

Run: `cargo build`
Expected: 成功，无错误

- [ ] **Step 12: 提交**

```bash
git add ecat-metrics/Cargo.toml ecat-metrics/src/lib.rs ecat-transport-http/Cargo.toml ecat-transport-http/src/lib.rs
git commit -m "ecat-metrics/transport-http: HttpServer 自动挂载 /metrics 端点"
```

---

### Task 3: config-remote watch — Consul 阻塞查询

**Files:**
- Modify: `ecat-config-remote/Cargo.toml`、`ecat-config-remote/src/lib.rs`

- [ ] **Step 1: 改 ecat-config-remote/Cargo.toml**

追加：

```toml
[dev-dependencies]
axum.workspace = true
```

- [ ] **Step 2: 重构 fetch（改后现有测试须保持通过）**

`ecat-config-remote/src/lib.rs`：
- `use std::collections::HashMap;` 后加 `use std::time::Duration;`
- `pub struct ConsulConfigSource` 上方加 `#[derive(Clone)]`
- 原 `impl ConfigSource for ConsulConfigSource` 中的 `load()` 实现替换为 `fetch` + `watch` 两个方法（放在 `impl ConsulConfigSource` 内），`ConfigSource::load` 改为委托：

```rust
    /// 拉取 KV。index 为 Some 时使用 Consul 阻塞查询（index + wait=5m），
    /// 返回 (配置 map, 新 X-Consul-Index)。
    async fn fetch(
        &self,
        index: Option<&str>,
    ) -> Result<(HashMap<String, serde_json::Value>, Option<String>), ConfigError> {
        let url = format!("{}/v1/kv/{}", self.base_url, self.prefix);
        let mut builder = self.client.get(&url).query(&[("recurse", "true")]);
        if let Some(idx) = index {
            builder = builder.query(&[("index", idx), ("wait", "5m")]);
        }
        builder = builder.timeout(Duration::from_secs(330));
        if let Some(ref token) = self.token {
            builder = builder.header("X-Consul-Token", token);
        }

        let resp = builder
            .send()
            .await
            .map_err(|e| ConfigError::Other(format!("consul kv: {e}")))?;

        if !resp.status().is_success() {
            let body = resp.text().await.unwrap_or_default();
            return Err(ConfigError::Other(format!("consul kv failed: {body}")));
        }

        let new_index = resp
            .headers()
            .get("x-consul-index")
            .and_then(|h| h.to_str().ok())
            .map(str::to_string);

        let entries: Vec<ConsulKvEntry> = resp
            .json()
            .await
            .map_err(|e| ConfigError::Other(format!("consul parse: {e}")))?;

        let mut map = HashMap::new();
        for entry in entries {
            let key = entry
                .key
                .strip_prefix(&self.prefix)
                .unwrap_or(&entry.key)
                .trim_matches('/')
                .replace('/', ".");
            if let Some(decoded) = entry.decoded_value() {
                if let Ok(v) = serde_json::from_str(&decoded) {
                    map.insert(key, v);
                } else {
                    map.insert(key, serde_json::Value::String(decoded));
                }
            }
        }

        Ok((map, new_index))
    }

    /// 启动 Consul 阻塞查询 watch，配置变更通过 mpsc channel 推送。
    /// receiver 被 drop 后后台任务自动退出。
    pub fn watch(
        &self,
    ) -> tokio::sync::mpsc::Receiver<Result<HashMap<String, serde_json::Value>, ConfigError>> {
        let (tx, rx) = tokio::sync::mpsc::channel(8);
        let source = self.clone();
        tokio::spawn(async move {
            let mut index: Option<String> = None;
            loop {
                match source.fetch(index.as_deref()).await {
                    Ok((map, new_index)) => {
                        if index.as_deref() != new_index.as_deref() {
                            if tx.send(Ok(map)).await.is_err() {
                                break; // receiver dropped
                            }
                        }
                        index = new_index;
                    }
                    Err(e) => {
                        if tx.send(Err(e)).await.is_err() {
                            break;
                        }
                        tokio::time::sleep(Duration::from_secs(1)).await;
                    }
                }
            }
        });
        rx
    }
```

`ConfigSource` impl 改为：

```rust
#[async_trait]
impl ConfigSource for ConsulConfigSource {
    async fn load(&self) -> Result<HashMap<String, serde_json::Value>, ConfigError> {
        Ok(self.fetch(None).await?.0)
    }
}
```

（`fetch`/`watch` 放入 `impl ConsulConfigSource` 块，`new`/`token` 之后。）

- [ ] **Step 3: 运行确认重构无回归**

Run: `cargo test -p ecat-config-remote --lib`
Expected: 原 2 个测试 PASS

- [ ] **Step 4: 写失败测试（mock Consul 服务器 + watch 测试）**（`mod tests` 头部加 `use axum::extract::State;` 和 `use tokio::sync::watch;`，测试追加到末尾）

```rust
    async fn spawn_mock_consul() -> (String, watch::Sender<(u64, Vec<(String, String)>)>) {
        let (tx, rx) = watch::channel((
            1u64,
            vec![("app/key".to_string(), "{\"a\":1}".to_string())],
        ));
        let app = axum::Router::new()
            .route("/v1/kv/{prefix}", axum::routing::get(kv_handler))
            .with_state(rx);
        let listener = tokio::net::TcpListener::bind("127.0.0.1:0").await.unwrap();
        let addr = listener.local_addr().unwrap();
        tokio::spawn(async move {
            axum::serve(listener, app).await.unwrap();
        });
        (format!("http://{addr}"), tx)
    }

    async fn kv_handler(
        State(mut rx): State<watch::Receiver<(u64, Vec<(String, String)>)>>,
        axum::extract::Query(params): axum::extract::Query<HashMap<String, String>>,
    ) -> axum::response::Response {
        let requested: u64 = params.get("index").and_then(|i| i.parse().ok()).unwrap_or(0);
        if params.contains_key("index") {
            // 模拟 Consul 阻塞查询：等待状态变化或 1s 超时后返回
            let deadline = tokio::time::sleep(Duration::from_secs(1));
            tokio::pin!(deadline);
            loop {
                if rx.borrow().0 > requested {
                    break;
                }
                tokio::select! {
                    _ = &mut deadline => break,
                    _ = rx.changed() => {
                        if rx.borrow().0 > requested {
                            break;
                        }
                    }
                }
            }
        }
        let (cur_idx, entries) = rx.borrow().clone();
        let body: Vec<serde_json::Value> = entries
            .iter()
            .map(|(k, v)| {
                serde_json::json!({
                    "Key": k,
                    "Value": base64::engine::general_purpose::STANDARD.encode(v),
                })
            })
            .collect();
        let mut resp = axum::response::Response::new(axum::body::Body::from(
            serde_json::to_string(&body).unwrap(),
        ));
        resp.headers_mut()
            .insert("X-Consul-Index", cur_idx.to_string().parse().unwrap());
        resp
    }

    #[tokio::test]
    async fn watch_first_frame_and_change() {
        let (base_url, tx) = spawn_mock_consul().await;
        let source = ConsulConfigSource::new(base_url, "app");
        let mut rx = source.watch();
        let first = tokio::time::timeout(Duration::from_secs(3), rx.recv())
            .await
            .expect("first frame timed out")
            .unwrap()
            .unwrap();
        assert_eq!(first.get("key"), Some(&serde_json::json!({"a": 1})));

        tx.send((
            2u64,
            vec![("app/key2".to_string(), "{\"b\":2}".to_string())],
        ))
        .unwrap();
        let second = tokio::time::timeout(Duration::from_secs(3), rx.recv())
            .await
            .expect("change timed out")
            .unwrap()
            .unwrap();
        assert_eq!(second.get("key2"), Some(&serde_json::json!({"b": 2})));
        assert!(second.get("key").is_none());
    }

    #[tokio::test]
    async fn watch_same_index_dedupes() {
        let (base_url, _tx) = spawn_mock_consul().await;
        let source = ConsulConfigSource::new(base_url, "app");
        let mut rx = source.watch();
        tokio::time::timeout(Duration::from_secs(3), rx.recv())
            .await
            .expect("first frame timed out")
            .unwrap()
            .unwrap();
        // 状态不变：服务器 1s 后返回同 index，不应推送
        let result = tokio::time::timeout(Duration::from_millis(2000), rx.recv()).await;
        assert!(result.is_err(), "no message expected on same index");
    }
```

- [ ] **Step 5: 运行确认通过**

Run: `cargo test -p ecat-config-remote --lib`
Expected: 4 个测试全 PASS（含原 2 个与新增 2 个）

- [ ] **Step 6: 工作区编译检查**

Run: `cargo build`
Expected: 成功，无错误

- [ ] **Step 7: 提交**

```bash
git add ecat-config-remote/Cargo.toml ecat-config-remote/src/lib.rs
git commit -m "ecat-config-remote: Consul 阻塞查询 watch（mpsc 输出配置变更）"
```

---

### Task 4: ClickHouse TsdbClient — ecat-data-clickhouse

**Files:**
- Modify: `ecat-data-clickhouse/src/lib.rs`

- [ ] **Step 1: 写失败测试（纯函数）**（`mod tests` 中追加；文件顶部 `use ecat_data::{RdbmsClient, RdbmsError, Row};` 扩展为 `use ecat_data::{DataPoint, FieldValue, RdbmsClient, RdbmsError, Row, TsdbClient, TsdbError};`）

```rust
    #[test]
    fn quote_ident_escapes_backticks() {
        assert_eq!(quote_ident("cpu"), "`cpu`");
        assert_eq!(quote_ident("a`b"), "`a``b`");
    }

    #[test]
    fn field_type_maps_variants() {
        assert_eq!(field_type(&FieldValue::Float(1.0)), "Float64");
        assert_eq!(field_type(&FieldValue::Int(1)), "Int64");
        assert_eq!(field_type(&FieldValue::String("s".into())), "String");
        assert_eq!(field_type(&FieldValue::Bool(true)), "UInt8");
    }

    #[test]
    fn build_create_table_sql() {
        let sql = build_create_table(
            "cpu",
            &["host".to_string()],
            &[("usage".to_string(), "Float64")],
        );
        assert_eq!(
            sql,
            "CREATE TABLE IF NOT EXISTS `cpu` (`host` String, `usage` Float64, `timestamp` Int64 DEFAULT 0) ENGINE = MergeTree ORDER BY timestamp"
        );
    }

    #[test]
    fn build_insert_body_serializes_and_escapes() {
        let points = vec![
            DataPoint::new("cpu")
                .with_tag("host", "a`b,c")
                .with_field("usage", FieldValue::Float(0.5))
                .with_timestamp(100),
            DataPoint::new("cpu")
                .with_field("usage", FieldValue::Int(7))
                .with_timestamp(200),
        ];
        let body = build_insert_body(&points, &["host".to_string()], &["usage".to_string()]);
        let lines: Vec<&str> = body.lines().collect();
        assert_eq!(lines.len(), 2);
        assert_eq!(
            lines[0],
            r#"{"host":"a`b,c","usage":0.5,"timestamp":100}"#
        );
        assert_eq!(lines[1], r#"{"usage":7,"timestamp":200}"#);
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test -p ecat-data-clickhouse --lib`
Expected: FAIL（编译错误：`quote_ident`、`field_type`、`build_create_table`、`build_insert_body` 未定义）

- [ ] **Step 3: 实现纯函数**（`ecat-data-clickhouse/src/lib.rs`，`impl ClickhouseClient` 块之后、`impl RdbmsClient` 之前）

```rust
fn quote_ident(s: &str) -> String {
    format!("`{}`", s.replace('`', "``"))
}

fn field_type(v: &FieldValue) -> &'static str {
    match v {
        FieldValue::Float(_) => "Float64",
        FieldValue::Int(_) => "Int64",
        FieldValue::String(_) => "String",
        FieldValue::Bool(_) => "UInt8",
    }
}

fn field_to_json(v: &FieldValue) -> serde_json::Value {
    match v {
        FieldValue::Float(f) => serde_json::Number::from_f64(*f)
            .map(serde_json::Value::Number)
            .unwrap_or(serde_json::Value::Number(0.into())),
        FieldValue::Int(i) => serde_json::Value::Number((*i).into()),
        FieldValue::String(s) => serde_json::Value::String(s.clone()),
        FieldValue::Bool(b) => serde_json::Value::Bool(*b),
    }
}

fn build_create_table(
    measurement: &str,
    tag_keys: &[String],
    field_cols: &[(String, &'static str)],
) -> String {
    let mut cols: Vec<String> = tag_keys
        .iter()
        .map(|k| format!("{} String", quote_ident(k)))
        .collect();
    cols.extend(
        field_cols
            .iter()
            .map(|(k, ty)| format!("{} {ty}", quote_ident(k))),
    );
    cols.push("timestamp Int64 DEFAULT 0".into());
    format!(
        "CREATE TABLE IF NOT EXISTS {} ({}) ENGINE = MergeTree ORDER BY timestamp",
        quote_ident(measurement),
        cols.join(", ")
    )
}

fn build_insert_body(points: &[DataPoint], tag_keys: &[String], field_keys: &[String]) -> String {
    let mut out = String::new();
    for p in points {
        let mut obj = serde_json::Map::new();
        for k in tag_keys {
            if let Some(v) = p.tags.get(k) {
                obj.insert(k.clone(), serde_json::Value::String(v.clone()));
            }
        }
        for k in field_keys {
            if let Some(v) = p.fields.get(k) {
                obj.insert(k.clone(), field_to_json(v));
            }
        }
        if let Some(ts) = p.timestamp {
            obj.insert("timestamp".into(), serde_json::Value::Number(ts.into()));
        }
        out.push_str(&serde_json::to_string(&obj).unwrap());
        out.push('\n');
    }
    out
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cargo test -p ecat-data-clickhouse --lib`
Expected: 6 个测试全 PASS（含原 2 个）

- [ ] **Step 5: 重构 post 辅助方法（回归保持）**（`impl ClickhouseClient` 内，`apply_auth` 后加）

```rust
    fn post(&self, sql: &str, params: &[(&str, String)]) -> reqwest::RequestBuilder {
        let mut rb = self
            .client
            .post(&self.base_url)
            .header("Content-Type", "text/plain; charset=utf-8")
            .query(&[("database", self.database.clone())])
            .body(sql.to_string());
        for (k, v) in params {
            rb = rb.query(&[(*k, v.clone())]);
        }
        self.apply_auth(rb)
    }
```

`impl RdbmsClient` 中 `execute` 改为（`send_progress_in_http_headers=1` 参数保留原语义）：

```rust
    async fn execute(&self, sql: &str) -> Result<u64, RdbmsError> {
        let resp = self
            .post(sql, &[("send_progress_in_http_headers", "1".to_string())])
            .send()
            .await
            .map_err(|e| RdbmsError::Database(format!("ch: {e}")))?;
        if !resp.status().is_success() {
            return Err(RdbmsError::Database(resp.text().await.unwrap_or_default()));
        }
        // ClickHouse reports written/result rows in the X-ClickHouse-Summary
        // response header (enabled via send_progress_in_http_headers=1).
        // Falls back to 0 when the server does not send the header.
        let affected = resp
            .headers()
            .get("x-clickhouse-summary")
            .and_then(|h| h.to_str().ok())
            .and_then(|s| serde_json::from_str::<serde_json::Value>(s).ok())
            .and_then(|v| {
                v.get("written_rows")
                    .and_then(|n| n.as_str())
                    .map(String::from)
            })
            .and_then(|s| s.parse::<u64>().ok())
            .unwrap_or(0);
        Ok(affected)
    }
```

`query` 改为：

```rust
    async fn query(&self, sql: &str) -> Result<Vec<Row>, RdbmsError> {
        let resp = self
            .post(sql, &[("default_format", "JSONEachRow".to_string())])
            .send()
            .await
            .map_err(|e| RdbmsError::Database(format!("ch query: {e}")))?;
        let text = resp
            .text()
            .await
            .map_err(|e| RdbmsError::Database(format!("ch read: {e}")))?;
        let mut rows = Vec::new();
        for line in text.lines() {
            if line.trim().is_empty() {
                continue;
            }
            let v = serde_json::from_str::<serde_json::Value>(line).map_err(|e| {
                RdbmsError::Database(format!(
                    "ch query: unparseable row (first 200 bytes shown): {e}: {}",
                    line.chars().take(200).collect::<String>()
                ))
            })?;
            if let Some(obj) = v.as_object() {
                let cols: Vec<String> = obj.keys().cloned().collect();
                let vals: Vec<serde_json::Value> = obj.values().cloned().collect();
                rows.push(Row::new(cols, vals));
            }
        }
        Ok(rows)
    }
```

- [ ] **Step 6: 运行确认重构无回归**

Run: `cargo test -p ecat-data-clickhouse --lib`
Expected: 6 个测试全 PASS

- [ ] **Step 7: 实现 TsdbClient**（`impl RdbmsClient` 块之后）

`ClickhouseClient` 结构体加字段，并在 3 个构造器（`new`、`with_auth`、`from_config`）中初始化：

```rust
    created: std::sync::Mutex<std::collections::HashSet<String>>,
```

```rust
            created: std::sync::Mutex::new(std::collections::HashSet::new()),
```

新增：

```rust
#[async_trait]
impl TsdbClient for ClickhouseClient {
    async fn write(&self, points: &[DataPoint]) -> Result<(), TsdbError> {
        // 按 measurement 分组，保持首见顺序
        let mut order: Vec<String> = Vec::new();
        let mut groups: std::collections::HashMap<String, Vec<&DataPoint>> =
            std::collections::HashMap::new();
        for p in points {
            if !groups.contains_key(&p.measurement) {
                order.push(p.measurement.clone());
            }
            groups.entry(p.measurement.clone()).or_default().push(p);
        }

        for measurement in order {
            let pts = &groups[&measurement];
            // 列集合取本批全部点；同名 field 类型不一致时先见者胜（文档注明）
            let tag_keys: Vec<String> = pts
                .iter()
                .flat_map(|p| p.tags.keys().cloned())
                .collect::<std::collections::BTreeSet<_>>()
                .into_iter()
                .collect();
            let field_cols: Vec<(String, &'static str)> = {
                let mut m: std::collections::BTreeMap<String, &'static str> =
                    std::collections::BTreeMap::new();
                for p in pts {
                    for (k, v) in &p.fields {
                        m.entry(k.clone()).or_insert_with(|| field_type(v));
                    }
                }
                m.into_iter().collect()
            };
            let field_keys: Vec<String> = field_cols.iter().map(|(k, _)| k.clone()).collect();

            // 建表（每表只建一次，按 client 缓存；CREATE IF NOT EXISTS 幂等）
            let created = self.created.lock().unwrap_or_else(|e| e.into_inner());
            if !created.contains(&measurement) {
                let create = build_create_table(&measurement, &tag_keys, &field_cols);
                let resp = self
                    .post(&create, &[])
                    .send()
                    .await
                    .map_err(|e| TsdbError::Other(format!("ch create: {e}")))?;
                if !resp.status().is_success() {
                    return Err(TsdbError::Other(format!(
                        "ch create failed: {}",
                        resp.text().await.unwrap_or_default()
                    )));
                }
                created.insert(measurement.clone());
            }
            drop(created);

            let body = build_insert_body(pts, &tag_keys, &field_keys);
            let cols: Vec<String> = tag_keys
                .iter()
                .chain(field_keys.iter())
                .chain(std::iter::once(&"timestamp".to_string()))
                .map(|c| quote_ident(c))
                .collect();
            let insert = format!(
                "INSERT INTO {} ({}) FORMAT JSONEachRow",
                quote_ident(&measurement),
                cols.join(", ")
            );
            let resp = self
                .post(&insert, &[])
                .send()
                .await
                .map_err(|e| TsdbError::Other(format!("ch write: {e}")))?;
            if !resp.status().is_success() {
                return Err(TsdbError::Other(format!(
                    "ch write failed: {}",
                    resp.text().await.unwrap_or_default()
                )));
            }
        }
        Ok(())
    }

    async fn query(&self, query: &str) -> Result<serde_json::Value, TsdbError> {
        let resp = self
            .post(query, &[("default_format", "JSONEachRow".to_string())])
            .send()
            .await
            .map_err(|e| TsdbError::Other(format!("ch tsdb query: {e}")))?;
        if !resp.status().is_success() {
            return Err(TsdbError::Other(resp.text().await.unwrap_or_default()));
        }
        let text = resp
            .text()
            .await
            .map_err(|e| TsdbError::Other(format!("ch read: {e}")))?;
        let mut rows = Vec::new();
        for line in text.lines() {
            let line = line.trim();
            if line.is_empty() {
                continue;
            }
            let v: serde_json::Value = serde_json::from_str(line)
                .map_err(|e| TsdbError::Other(format!("ch tsdb parse: {e}")))?;
            rows.push(v);
        }
        Ok(serde_json::json!(rows))
    }

    async fn delete(&self, query: &str) -> Result<(), TsdbError> {
        // ClickHouse 轻量删除语法：ALTER TABLE <t> DELETE WHERE ...
        let resp = self
            .post(query, &[])
            .send()
            .await
            .map_err(|e| TsdbError::Other(format!("ch delete: {e}")))?;
        if !resp.status().is_success() {
            return Err(TsdbError::Other(format!(
                "ch delete failed: {}",
                resp.text().await.unwrap_or_default()
            )));
        }
        Ok(())
    }
}
```

- [ ] **Step 8: 运行确认通过**

Run: `cargo test -p ecat-data-clickhouse --lib`
Expected: 6 个测试全 PASS

- [ ] **Step 9: 工作区编译检查**

Run: `cargo build`
Expected: 成功，无错误

- [ ] **Step 10: 提交**

```bash
git add ecat-data-clickhouse/src/lib.rs
git commit -m "ecat-data-clickhouse: 实现 TsdbClient（自动建表 + JSONEachRow 写入）"
```

---

## 自检记录（writing-plans skill 要求）

**Spec 覆盖：**
- metrics 自动端点 → Task 2（metrics_router + HttpServer 自动挂载）✓
- openapi PUT/DELETE/PATCH/HEAD/OPTIONS → Task 1 ✓
- config-remote Consul 阻塞查询 watch → Task 3（fetch 重构 + watch + mock 测试：首帧/变更/去重）✓
- ClickHouse TsdbClient → Task 4（write 自动建表、query JSON 数组、delete）✓

**类型一致性：** `quote_ident`/`field_type`/`field_to_json`/`build_create_table`/`build_insert_body` 签名在测试与实现中一致；`watch()` 返回 `mpsc::Receiver<Result<HashMap<String, serde_json::Value>, ConfigError>>`；`metrics_router()` 返回 `Router`。

**注意（相对 spec 的实现调整）：** 建表缓存由 spec 的 `OnceLock<HashMap>` 改为 `ClickhouseClient` 实例字段 `Mutex<HashSet<String>>`——避免跨 client/跨 database 误跳过建表（CREATE IF NOT EXISTS 本身幂等，缓存仅为省一次往返）。
